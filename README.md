# PLN: Género de Películas

```
python -m venv ".env"                   # Creación de Entorno Virtual
.env\Scripts\activate                   # Activar Entorno Virtual (Manual)
pip install -r requirements.txt         # Instalar dependencias
pip freeze > requirements.txt           # Transferir nombres y versiones usadas de módulos a: requirements.txt
```
